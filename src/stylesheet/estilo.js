import { StyleSheet } from 'react-native';

export const estilos = StyleSheet.create({
  conteudo: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },

  titulo: {
    fontSize: 28,
    color: 'darkred',
    marginBottom: 20,
    fontWeight: 'bold',
  },
});